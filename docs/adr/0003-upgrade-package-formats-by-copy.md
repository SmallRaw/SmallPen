# Upgrade Package formats by copy

When a future SmallPen format requires migration, the upgrade produces a new Package and preserves the original unchanged. This costs additional disk space but keeps real user projects recoverable and avoids making a newer tool version an irreversible boundary.
